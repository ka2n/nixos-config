# Copy to laptop.nix and fill in your values
# Then: git add -f -N private/laptop.nix && git update-index --assume-unchanged private/laptop.nix
{
  domains = ["your-tenant.onmicrosoft.com"];
  username = "user@example.com";
}
